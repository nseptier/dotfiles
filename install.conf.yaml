- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/git: config/git
    ~/.config/nvim: config/nvim
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zsh: zsh
    ~/.zshrc: zshrc
